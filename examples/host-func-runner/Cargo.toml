[package]
name = "host-func-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
host-func-abi = { path = "../host-func-abi" }
wasm-plugins = { path = "../../lib", features = ["wasmer-rt-sys"] }

semver = "^1.0"
